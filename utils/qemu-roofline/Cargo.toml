[package]
name = "miniperf-qemu-roofline"
version = "0.1.0"
edition = "2021"

[lib]
name = "miniperf_qemu_roofline"
crate-type = ["cdylib"]

[build-dependencies]
serde_json = "1.0"

[dev-dependencies]
serde_json = "1.0"
